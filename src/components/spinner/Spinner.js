import React from 'react';
import style from './Spinner.module.css';

/**
 * @name Spinner
 * @description style wrapper for loading spinner
 * @return component
 */
const Spinner = () => <div className={style.loader} />;

export default Spinner;
